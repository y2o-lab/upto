{
  "name": "upto",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "build-storybook": "pnpm --filter @upto/web build-storybook",
    "db:generate": "pnpm --filter @upto/db db:generate",
    "db:migrate": "pnpm --filter @upto/db db:migrate",
    "dev": "pnpm --parallel --filter @upto/web --filter @upto/collector dev",
    "dev:collector": "pnpm --filter @upto/collector dev",
    "dev:web": "pnpm --filter @upto/web dev",
    "format": "oxfmt . \"!docs/**\" \"!AGENTS.md\"",
    "format:check": "oxfmt --check . \"!docs/**\" \"!AGENTS.md\"",
    "lint": "oxlint .",
    "lint:fix": "oxlint . --fix",
    "storybook": "pnpm --filter @upto/web storybook",
    "test": "vitest run",
    "test:watch": "vitest",
    "trigger:deploy:dry-run": "trigger deploy --dry-run --skip-update-check --skip-telemetry",
    "trigger:deploy:prod": "trigger deploy --env prod --skip-update-check --skip-telemetry",
    "trigger:deploy:staging": "trigger deploy --env staging --skip-update-check --skip-telemetry",
    "trigger:dev": "trigger dev --skip-update-check --skip-telemetry",
    "typecheck": "tsc -b",
    "verify": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm test"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0",
    "@trigger.dev/sdk": "4.5.7",
    "@types/node": "25.9.3",
    "@types/react": "19.2.17",
    "@types/react-dom": "19.2.3",
    "jsdom": "29.1.1",
    "oxfmt": "0.60.0",
    "oxlint": "1.75.0",
    "trigger.dev": "4.5.7",
    "tsx": "4.23.1",
    "typescript": "6.0.3",
    "vitest": "4.1.10"
  },
  "engines": {
    "node": ">=22.14.0",
    "pnpm": ">=10.8.1"
  },
  "packageManager": "pnpm@10.8.1"
}
